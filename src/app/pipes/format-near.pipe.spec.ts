import { FormatNearPipe } from './format-near.pipe';

describe('FormatNearPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatNearPipe();
    expect(pipe).toBeTruthy();
  });
});
